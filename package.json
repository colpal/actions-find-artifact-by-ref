{
  "scripts": {
    "watch": "npm run build -- --watch=forever",
    "build": "esbuild index.mjs --bundle --outfile=out.js --platform=node --target=node20"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.20.1",
    "globals": "^15.15.0"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "@octokit/plugin-throttling": "^11.0.3"
  },
  "version": "2.2.5",
  "volta": {
    "node": "20.18.3"
  }
}
