{
  "scripts": {
    "build": "esbuild index.mjs --bundle --outfile=out.js --platform=node --target=node16",
    "prepare": "husky install"
  },
  "devDependencies": {
    "esbuild": "^0.14.21",
    "eslint": "^8.9.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.25.4",
    "husky": "^7.0.4"
  },
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/github": "^5.0.0"
  }
}
