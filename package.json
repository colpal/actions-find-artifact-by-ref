{
  "scripts": {
    "build": "esbuild index.js --bundle --minify --outfile=out.js --platform=node --target=node16",
    "prepare": "husky install"
  },
  "devDependencies": {
    "esbuild": "^0.14.21",
    "eslint": "^8.8.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.25.4",
    "husky": "^7.0.4"
  }
}
